server:
  port: 7999
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/finalcola/green-collection-config.git
          username: 18080879806m@sina.cn
          password: z845447141
          search-paths: config
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/

